# Ensures production DB schema matches prisma/schema before traffic hits the app.
# See https://docs.railway.app/deployments/pre-deploy-command
[deploy]
# Railway allows at most one array element here; use a single shell string.
preDeployCommand = "npm run db:deploy"
startCommand = "npm run start"
